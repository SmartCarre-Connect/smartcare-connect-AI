# Production env used for build-time defaults (committed only for demo presentation)
VITE_RESTRICT_TO_DEMO=true
# Backend API (keep default; change in Vercel if using live backend)
VITE_API_URL=https://smartcare-connect-api.onrender.com/api/v1
